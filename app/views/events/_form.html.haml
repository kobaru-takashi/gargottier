= form_for @event do |f|
  .field
    = f.label :タイトル
    = f.text_field :title
  -# .field
  -#   = f.label :内容
  -#   = f.text_field :body
  .field
    = f.label :日付
    = f.date_select :date
  .actions
    = f.submit '登録'